import React from 'react';
import PromotionAnimation from './Enrol';

const Promotion = () => {
    return (
        <div className="promotion_wrapper" style={{ background: '#ffffff' }}>
            <div className="container">
                <Promotion />
            </div>
        </div>
    );
};

export default Promotion;