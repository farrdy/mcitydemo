import React from 'react';
import React from 'react';
import { Route, Redirect } from 'react-router-dom'
const PublicRoutes = ({
    user,
    component: Comp,
    ...rest
}) => {
    return (
        <div>

        </div>
    );
};

export default PublicRoutes;